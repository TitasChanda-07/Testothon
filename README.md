# Testothon
I have Built a complete solution that: Analyzes JSONL records of test failures (Training data file). Classifies failures into categories. Identify failures from training data. Generates reasoning for each classification. Generate a triage report/dashboard that gives complete information. Also, Correlate failures with historical defect tickets
